// 复用共享 lint 配置（open/config · @lieshoucloud/contract-config）
// files 模式相对运行目录解析，从本仓 CWD 执行时匹配本仓 src/**/*.ts
import shared from "@lieshoucloud/contract-config/eslint.config.js";

export default shared;
